version: "3.1"

rules:
  - rule: "Greeting Rule"
    steps:
      - intent: greet
      - action: utter_greet

  - rule: "Say goodbye anytime the user says goodbye"
    steps:
      - intent: goodbye
      - action: utter_goodbye